version: '3'
services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      # 移除MongoDB连接配置
    # 移除MongoDB依赖
    restart: always

# 移除MongoDB服务和卷配置
